name: iOS Push Testing Build
on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Cache Node Modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Cache Pods
        uses: actions/cache@v4
        with:
          path: ios/Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('**/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Setup EAS CLI
        run: npm install -g eas-cli@latest

      - name: Install dependencies
        run: npm install

      # 1. 다시 --local 플랜으로 우회하여 무료 빌드를 수행합니다.
      - name: Build Local Tarball
        run: eas build --platform ios --profile development --local --non-interactive
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          APP_STORE_CONNECT_API_KEY_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_KEY_ID }}
          APP_STORE_CONNECT_API_KEY_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_KEY: ${{ secrets.APP_STORE_CONNECT_API_KEY_KEY }}
          SENTRY_DISABLE_AUTO_UPLOAD: true

      # 2. [핵심] 가상 Mac 안에서 깨끗하게 .tar.gz 압축을 풀고 정품 .ipa 포맷으로 조립합니다.
      - name: Package IPA on macOS Runner
        run: |
          mkdir -p dist/Payload
          tar -zxf build-*.tar.gz -C dist/Payload
          mv dist/Payload/build-* dist/Payload/xtrusfcm.app
          cd dist
          zip -r xtrusfcm.ipa Payload
          
      # 3. 완성된 순정 .ipa 파일만 대시보드에 업로드합니다.
      - name: Upload Finished IPA
        uses: actions/upload-artifact@v4
        with:
          name: ios-ready-ipa
          path: dist/xtrusfcm.ipa
